/**
 * Legacy re-export. The real planet palette lives at @/lib/planets so
 * every future call site has one place to pull from. Do not duplicate
 * colors — edit lib/planets.ts instead.
 */
export { PLANET_COLORS } from "@/lib/planets";
